<div class="container mt-4">
<div class="row">
<div class="mx-auto col-10 col-xl-6 col-lg-7 col-sm-9 pt-4">
  <h1 class="mt-3 border-bottom text-center mb-5">商品詳細</h1>
  
  <span class="mt-5 border-bottom">商品名</span>
  <p><%= @item.name %></p>
  
  <span class="mt-5 border-bottom">商品説明</span>
  <p><%= @item.introduction %></p>
  
  <span class="mt-5 border-bottom">画像</span>
  <p><%= image_tag @item.get_image, size:"200x200"%></p>
  
  <span class="mt-5 border-bottom">ジャンル</span>
  <p><%= @item.genre.name %></p>
  
  <span class="mt-5 border-bottom">商品価格(税抜)</span>
  <p><%= @item.price %></p>
  
  
  <p class="text-center bg-primary rounded mt-5">
  <%= link_to edit_admin_item_path(@item.id), class: "text-light" do %>編集
  <% end %>
  </p>
  
  <p class="text-center bg-warning rounded mt-2">
  <%= link_to admin_items_path, class: "text-light" do %>戻る
  <% end %>
  </p>
  
</div>
</div>
</div>